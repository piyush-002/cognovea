node_modules/
.next/
out/
.DS_Store
*.tsbuildinfo
next-env.d.ts
.env
.env.*
# ...but the blank template is meant to be committed.
!.env.example

# Local uploads (development only, production uses Vercel Blob)
public/uploads/

# Verification harness output
preview/
.preview-bundle.cjs

# Payload generates this; it is committed on purpose, so it is NOT ignored:
#   src/payload-types.ts

# Transfer archives
*.tgz

# Scratch: files staged for deletion. This VM cannot unlink, so they are
# moved here rather than removed. Safe to delete the whole folder.
_to_delete/
